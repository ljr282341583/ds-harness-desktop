# CLAUDE.md

本仓库的代理启动指引维护在 `AGENTS.md`。

Claude Code 在开始本仓库的任何工作前，请先阅读 `AGENTS.md`，并以它作为唯一的主指引文件。

`AGENTS.md` 包含 Agent Loop Bootstrap Protocol、Agent Ownership、Message Intent Guard、Workflow Gateway Map、Gate Modes、Required Stops、Completion Rules、Submit And Commit Rules、Project Memory And Artifacts 等章节，以及本项目的架构快照、命令与硬约束。

不要在本文件中重复长期工作流规则；保持它为一个指针，让所有代理 CLI 共享同一份维护源。

补充说明：本项目的 agent-loop 记忆根不在本仓库内，位于 Codex 工作空间的项目目录 `projects/dsh-desktop-updater/.agent-loop/`。
