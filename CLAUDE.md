# CLAUDE.md

本仓库的代理启动指引维护在 `AGENTS.md`。

Claude Code 在开始本仓库的任何工作前，请先阅读 `AGENTS.md`，并以它作为唯一的主指引文件。

`AGENTS.md` 包含工作方式、记忆与交接、验证与提交规则，以及本项目的仓库速览、架构快照、目录指引、项目命令与 8 条硬约束。**本项目不使用 `agent-loop`**，也没有外部 `.agent-loop/` 记忆根。

不要在本文件中重复长期工作流规则；保持它为一个指针，让所有代理 CLI 共享同一份维护源。
