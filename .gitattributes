# 统一行尾：仓库内一律以 LF 存储，工作区也检出为 LF。
# 目的：避免 CRLF/LF 混合导致「工作区文件内容」与「git 索引内容」不一致
#（曾因此出现提交内容与预期不一致的问题）。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.ico binary
*.png binary
*.exe binary
*.dll binary
*.zip binary
*.7z binary
